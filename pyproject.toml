[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cyt-ng"
version = "0.1.0"
description = "Chasing Your Tail NG — headless EDC personal SIGINT platform"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "requests>=2.28.0",
  "cryptography>=40.0.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0.0", "ruff>=0.4.0"]

[project.scripts]
# `cyt-analyzer` is the historical name; `cyt` is the short operator form
# used throughout the docs (README, FIELD_DEPLOY). Both run the same entry.
cyt = "cyt_platform.__main__:main"
cyt-analyzer = "cyt_platform.__main__:main"

[tool.setuptools.packages.find]
include = ["cyt_platform*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# "." — the project root (cyt_platform + configs). "legacy" — quarantined
# legacy tools stay importable under their historical top-level names so
# their tests keep running green (locked decision 9).
pythonpath = [".", "legacy"]

[tool.ruff]
# Lint gate is scoped to the platform package and its tests. Legacy tools are
# quarantined under legacy/ (locked decision 2) and stay out of the gate;
# the shared modules that have a proven runtime role moved into cyt_platform/
# and are linted like the rest of the package.
include = ["cyt_platform/**/*.py", "tests/**/*.py"]
# Python 3.9 is the supported floor (requires-python); pin it so ruff never
# suggests 3.10+ syntax on hot paths.
target-version = "py39"

[tool.ruff.lint]
# Pin ruff's documented default rule set (pycodestyle errors + pyflakes)
# explicitly so CI is deterministic across ruff releases; broader categories
# (pyupgrade, flake8-*, etc.) are opt-in later, not ruff-default drift.
select = ["E4", "E7", "E9", "F"]
